# Core dependencies - using compatible versions for Streamlit Cloud
langchain==0.3.0
langchain-community==0.3.0
langchain-core==0.3.0

# PDF processing - using versions that work well on Streamlit Cloud
pymupdf==1.23.21
pdfplumber==0.10.4

# OCR - optional for Streamlit Cloud deployment
# pytesseract==0.3.10
Pillow==10.0.1

# Vector database - faiss-cpu is more compatible than full faiss
faiss-cpu==1.7.4
chromadb==0.4.22

# Ollama integration - may need to be configured for remote access on Streamlit Cloud
ollama==0.1.7
# Using compatible pydantic version
pydantic==2.7.4

# Template and report generation
jinja2==3.1.2

# Web interface
streamlit==1.28.0

# Utilities - using versions compatible with Streamlit Cloud
python-dotenv==1.0.0
numpy==1.24.3
pandas==2.1.1
scikit-learn==1.3.0

# Testing - not needed for deployment
# pytest==7.4.2